#include <cstdio>

int main(int argc, char *argv[]) {
  int N = 10;
  int *a = new int[N];
  for(int i = 0; i < N; i++){
    a[i] = 1;
  }
  
  printf("Value stored in p: %d\n", *p);

  delete[] a;
  
  return 0;
}
